/**
 * @jsx React.DOM
 */

var React = require('react/addons');

var api = require('../utility/api-endpoints');


var Done = React.createClass({
  render: function() {
    return (
      <div>done</div>
    );
  }
});

module.exports = Done;
